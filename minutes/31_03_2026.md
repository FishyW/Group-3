Agenda:
- Go through the assignment
  - PLAN FOR MODULARITY.
- "Assign roles"
- Set a timeline

Discussion:

Going through the assignment:
1. Exercise 1: Digital I/O
CREATE A MODULE THAT CAN INTERFACE TO LED AND BUTTON.

  a) Basic functionality: Module to set up any GPIO pin for input / output and allow read from GPIO pins.
  Digital read / write -- I.E. "HIGH / LOW"
  b) "Encapsulate Interfaces" -- Specific modules for A and E to control button and LED, (Also C if needed ig?)
  c) 'Software Module' --> main, give it a function, when button call back??
    "Callback" --> Function that program calls when event.
    - What's the use?

    Seems to just become an "if" --> But ig if there's more conditions it's cleaner.

    "Decouples the button module from the code that needs to respond"

    "The idea is: Inversion of Control"
      Default: 

  ```
  Button file:

  button_turn_on_led:
    Turn on LED

  vs

  Button file:

  Button_ON
    code to check if button on

  main file

  def button_do_function(Function callback)
    if Button_ON():
      callback

  button_do_function(Turn on LED)
  ```
  d) get/set functions?
  e) Restrict the speed LEDs change. Throttle? --> give cooldown.

2. Exercise 2: Timer Interface
Module to hardware timers.
Make a PWM driver to servo

  a) Timer software to trigger callback with customizable time.
  b) Reset with new period, not accessible outside the module scope.
  c) PWM SIGNAL: timer + GPIO for timer 
  "Let's study the servo motor."
  d) one shot event - 

3. Exercise 3: Serial Interface
Sending or receiving, and strings to serial port.

a) UART to send and receive bytes, with known structure.
b) sendString --> USART1
c) Sending a structure through serial. HAVE start, message, message type, body, stop, checksum.
d) Receive structure, and do a callback function.
e) Replace receive with interrupt.
f) Interrupt to transmit. Continue to receive while a different function uses the string.

4. Exercise 4: I2C Sensor Interfacing.
Using I2C to communicate. COMPASS DATA

a) implement I2C interface to compass
b) Make structure to store values returned by magneto meter. Conditions on the data:
c) Accelerometer --> Attitude reference estimator.

5. EXERCISE 5 INTEGRATION TASK
Use all the modules to do something.

a) read magneto meter data from STM, package into a datastructure, send over UART.

b) interrupt button function on 1 -- tells 2nd board how to display

c) Change position of servo in response to heading from 1st board. 2 modes: servo or LED
